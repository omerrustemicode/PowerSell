<Window x:Class="PowerSell.Views.ClientView.SingleClientWindow"
        xmlns:local="clr-namespace:PowerSell.Localization"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" WindowState="Maximized">
    <Window.Resources>
        <local:LocalizationManager x:Key="LocalizationManager"/>
    </Window.Resources>

    <Grid>
        <!-- Left Side -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Side Content - DataGrid -->
        <DataGrid x:Name="serviceDataGrid" Grid.Column="0" Background="#222222">
            <!-- You can customize the DataGrid as needed -->
        </DataGrid>

        <!-- Middle Line (GridSplitter) -->
        <GridSplitter Grid.Column="1" Width="5" Background="#333333" VerticalAlignment="Stretch" HorizontalAlignment="Left"/>

<<<<<<< HEAD
    <!-- Right Side -->
<StackPanel Grid.Column="2" Background="#444444">
    <!-- Fixed-size buttons -->
    <WrapPanel>
        <Button Content="{Binding Source={StaticResource LocalizationManager}, Path=Transport}" Width="50" Height="50" Margin="5" Background="#666666" Foreground="White" Click="Transport_Btn"/>
        <Button Content="{Binding Source={StaticResource LocalizationManager}, Path=Bill}" Width="50" Height="50" Margin="5" Background="#666666" Foreground="White" Click="Button_Click"/>
        <Button Content="{Binding Source={StaticResource LocalizationManager}, Path=Order}" Width="50" Height="50" Margin="5" Background="#666666" Foreground="White" Click="Button_Click"/>
        <Button Content="{Binding Source={StaticResource LocalizationManager}, Path=PrintService}" Width="50" Height="50" Margin="5" Background="#666666" Foreground="White" Click="Button_Click"/>
    </WrapPanel>
=======
        <!-- Right Side -->
        <StackPanel Grid.Column="2" Background="#444444">
            <!-- Fixed-size buttons -->
            <WrapPanel>
                <Button Content="{Binding Source={StaticResource LocalizationManager}, Path=Transport}" Width="50" Height="50" Margin="5" Background="#666666" Foreground="White" Click="Transport_Btn"/>
                <Button Content="{Binding Source={StaticResource LocalizationManager}, Path=Bill}" Width="50" Height="50" Margin="5" Background="#666666" Foreground="White" Click="Button_Click"/>
                <Button Content="{Binding Source={StaticResource LocalizationManager}, Path=Order}" Width="50" Height="50" Margin="5" Background="#666666" Foreground="White" Click="Button_Click"/>
                <Button Content="{Binding Source={StaticResource LocalizationManager}, Path=PrintService}" Width="50" Height="50" Margin="5" Background="#666666" Foreground="White" Click="Button_Click"/>
            </WrapPanel>
>>>>>>> 037921e4684cc3ef4e0f64124da16e6a8b662711

    <!-- Separator Line with fixed width -->
    <Separator Width="Auto" Height="1" Background="White" Margin="0,5,0,5"/>

    <!-- More buttons or customize as needed -->
    <WrapPanel>
        <Button Content="Button 4" Width="50" Height="50" Margin="5" Background="#666666" Foreground="White" Click="Button_Click"/>
        <Button Content="Button 5" Width="50" Height="50" Margin="5" Background="#666666" Foreground="White" Click="Button_Click"/>
    </WrapPanel>
</StackPanel>

    </Grid>
</Window>
